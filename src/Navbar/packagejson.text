"proxy":"http://localhost:3030",

"eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },

here after connecting mysql remove this in package.json file
-------------------------------------------------